version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto21
  build:
    commands:
      - echo Build completed on `date`
      - cd JavaWeb-master-MySQL
      - mvn package
  post_build:
    commands:
      - echo Build completed on `data`
artifacts:
  files:
    - JavaWeb-master-MySQL/target/*.jar
  discard-paths: yes